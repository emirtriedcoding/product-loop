import Saved from "@/components/user/saved/Saved";

export const metadata = {
  title: "حساب کاربری - ذخیره شده ها",
};

const SavedPage = async () => {
  return <Saved />;
};

export default SavedPage;
